<section class="hero" aria-labelledby="hero-title"><h1 id="hero-title">Impact<br />Festival</h1><div class="meta"><span>Trifolium</span><span>11–12 Sep 2026</span></div><p>Music // Art // Talks // Workshops // Food<br />Designed to increase our impact</p><a id="tickets" class="button" href="#newsletter">Tickets</a></section>
<style>
	.hero { min-height:38.75rem; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:1.25rem; padding:3.5rem 1.25rem 2.75rem; text-align:center; background:linear-gradient(#050805,#384224 50%,#1a120a); color:var(--brand-red); }
	h1 { margin:0; font-family:var(--font-display); font-size:clamp(4rem,17vw,9.25rem); line-height:.82; letter-spacing:-.05em; text-transform:uppercase; }
	.meta { display:flex; justify-content:center; gap:clamp(2rem,14vw,22.5rem); width:100%; font-weight:600; font-size:clamp(.9rem,2.5vw,1.8rem); text-transform:uppercase; white-space:nowrap; }
	p { margin:0; font-weight:600; line-height:1.45; text-transform:uppercase; }
	.button { display:inline-flex; min-height:3.25rem; align-items:center; justify-content:center; padding:.9rem 2rem; color:#fff; background:var(--brand-red); border-radius:2rem; font-weight:600; text-decoration:none; text-transform:uppercase; }
	@media (min-width:48rem) { .hero { min-height:56.25rem; padding-inline:6rem; } .button { border-radius:0; } }
</style>
